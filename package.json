{
  "name": "curriculum-management-system",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm --prefix frontend install --include=dev --no-audit --no-fund && npm --prefix frontend run build && npm --prefix backend install --omit=dev --no-audit --no-fund",
    "start": "npm --prefix backend start"
  }
}
